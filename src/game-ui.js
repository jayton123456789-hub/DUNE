(function attachGameUI(root,factory){root.DriftGameUI=factory();})(typeof globalThis!=='undefined'?globalThis:this,function createModule(){'use strict';const $=id=>document.getElementById(id);const SKINS=[{id:'aqua',name:'Aqua',price:0,hue:0},{id:'coral',name:'Coral',price:25,hue:145},{id:'sunset',name:'Sunset',price:50,hue:195},{id:'violet',name:'Violet',price:80,hue:75},{id:'lime',name:'Lime',price:120,hue:260},{id:'midnight',name:'Midnight',price:180,hue:35}];const SAVE_KEY='driftline-save-v6';const defaults=()=>({best:0,wallet:0,lifetimeCoins:0,runs:0,perfects:0,owned:['aqua'],skin:'aqua',settings:{sound:true,vibration:true,motion:true}});function loadSave(){try{const raw=JSON.parse(localStorage.getItem(SAVE_KEY)||localStorage.getItem('driftline-save-v5')||'{}');const base=defaults();return{...base,...raw,owned:raw.owned?.length?raw.owned:['aqua'],settings:{...base.settings,...(raw.settings||{})}};}catch(_){return defaults();}}class AudioBus{constructor(settings){this.settings=settings;}unlock(){if(!this.settings().sound)return;if(!this.context){const AC=window.AudioContext||window.webkitAudioContext;if(!AC)return;this.context=new AC();this.master=this.context.createGain();this.master.gain.value=0.12;this.master.connect(this.context.destination);}if(this.context.state==='suspended')this.context.resume();}tone(start,duration,type='sine',gain=0.08,end=start){if(!this.settings().sound)return;this.unlock();if(!this.context)return;const now=this.context.currentTime;const oscillator=this.context.createOscillator();const amplifier=this.context.createGain();oscillator.type=type;oscillator.frequency.setValueAtTime(start,now);oscillator.frequency.exponentialRampToValueAtTime(Math.max(35,end),now+duration);amplifier.gain.setValueAtTime(0.0001,now);amplifier.gain.exponentialRampToValueAtTime(gain,now+0.01);amplifier.gain.exponentialRampToValueAtTime(0.0001,now+duration);oscillator.connect(amplifier);amplifier.connect(this.master);oscillator.start(now);oscillator.stop(now+duration+0.02);}click(){this.tone(510,0.055,'triangle',0.06,690);}launch(){this.tone(175,0.16,'triangle',0.09,460);}coin(){this.tone(910,0.08,'sine',0.1,1360);}land(grade){grade==='perfect'?this.tone(520,.13,'triangle',.1,840):this.tone(grade==='good'?350:130,.1,'triangle',.06,grade==='good'?470:82);}crash(){this.tone(110,.24,'sawtooth',.13,48);}}function create(){const U={hud:$('hud'),score:$('scoreValue'),runCoins:$('runCoins'),toast:$('landingToast'),multiplier:$('multiplierValue'),speed:$('speedLabel'),altitude:$('altitudeLabel'),pause:$('pauseButton'),menu:$('menuRoot'),wallet:$('walletCoins'),shopWallet:$('shopWalletCoins'),bestMenu:$('bestScore'),skinName:$('selectedSkinName'),play:$('playButton'),shop:$('shopGrid'),sound:$('soundToggle'),vibration:$('vibrationToggle'),motion:$('motionToggle'),reset:$('resetDataButton'),statsBest:$('statsBest'),statsRuns:$('statsRuns'),statsCoins:$('statsCoins'),statsPerfects:$('statsPerfects'),statsSkins:$('statsSkins'),over:$('gameOverScreen'),overTitle:$('gameOverTitle'),overReason:$('gameOverReason'),final:$('finalScore'),finalBest:$('finalBest'),finalCoins:$('finalCoins'),retry:$('retryButton'),resultShop:$('resultShopButton'),main:$('menuButton'),pauseOverlay:$('pauseOverlay'),resume:$('resumeButton'),restart:$('restartButton'),pauseSettings:$('pauseSettingsButton'),pauseMenu:$('pauseMenuButton'),system:$('toastMessage')};let save=loadSave();const persist=()=>{try{localStorage.setItem(SAVE_KEY,JSON.stringify(save));}catch(_){}};const selectedSkin=()=>SKINS.find(skin=>skin.id===save.skin)||SKINS[0];const audio=new AudioBus(()=>save.settings);const haptic=pattern=>{if(save.settings.vibration&&navigator.vibrate)navigator.vibrate(pattern);};const quip=(message,color='#fff',duration=850)=>{U.toast.textContent=message;U.toast.style.color=color;U.toast.classList.add('show');clearTimeout(quip.timer);quip.timer=setTimeout(()=>U.toast.classList.remove('show'),duration);};const systemToast=message=>{U.system.textContent=message;U.system.classList.add('show');clearTimeout(systemToast.timer);systemToast.timer=setTimeout(()=>U.system.classList.remove('show'),1300);};function gameplayVisible(visible,state){U.hud.classList.toggle('active',visible);U.hud.setAttribute('aria-hidden',visible?'false':'true');U.pause.style.display=visible&&state==='playing'?'block':'none';}function renderSettings(){U.sound.classList.toggle('on',save.settings.sound);U.vibration.classList.toggle('on',save.settings.vibration);U.motion.classList.toggle('on',save.settings.motion);}function renderShop(){U.shop.replaceChildren();for(const skin of SKINS){const owned=save.owned.includes(skin.id),equipped=save.skin===skin.id;const card=document.createElement('article');card.className=`shop-card${equipped ? ' selected' : ''}${owned ? '' : ' locked'}`;card.innerHTML=`<div class="skin-preview"><span class="skin-ball"></span></div><h3>${skin.name}</h3><p>Cosmetic colorway</p><button class="shop-action" data-skin="${skin.id}">${equipped ? 'EQUIPPED' : owned ? 'EQUIP' : `🪙${skin.price}`}</button>`;U.shop.append(card);}}function render(){const skin=selectedSkin();U.wallet.textContent=U.shopWallet.textContent=save.wallet;U.bestMenu.textContent=U.statsBest.textContent=`${Math.floor(save.best)} m`;U.skinName.textContent=skin.name;U.statsRuns.textContent=save.runs;U.statsCoins.textContent=save.lifetimeCoins;U.statsPerfects.textContent=save.perfects;U.statsSkins.textContent=`${save.owned.length} / ${SKINS.length}`;renderSettings();renderShop();}function activateView(name){document.querySelectorAll('.menu-view').forEach(view=>{const active=view.dataset.view===name;view.classList.toggle('active',active);view.setAttribute('aria-hidden',active?'false':'true');});}function openMenu(name='main'){U.pauseOverlay.classList.remove('active');U.over.classList.remove('active');U.menu.classList.add('active');activateView(name);render();}function closeOverlays(){U.menu.classList.remove('active');U.over.classList.remove('active');U.pauseOverlay.classList.remove('active');}function bank(score,coins){save.wallet+=coins;save.lifetimeCoins+=coins;save.runs+=1;save.best=Math.max(save.best,Math.floor(score));persist();}function showGameOver(title,reason,score,coins){U.overTitle.textContent=title;U.overReason.textContent=reason;U.final.textContent=`${Math.floor(score)} m`;U.finalBest.textContent=`${Math.floor(save.best)} m`;U.finalCoins.textContent=coins;U.over.classList.add('active');}function bind(callbacks){document.querySelectorAll('[data-open-view]').forEach(button=>button.addEventListener('click',()=>{audio.click();activateView(button.dataset.openView);render();}));document.querySelectorAll('[data-setting]').forEach(button=>button.addEventListener('click',()=>{const key=button.dataset.setting;save.settings[key]=!save.settings[key];persist();renderSettings();}));U.shop.addEventListener('click',event=>{const button=event.target.closest('[data-skin]');if(!button)return;const skin=SKINS.find(item=>item.id===button.dataset.skin);if(!skin)return;if(save.owned.includes(skin.id))save.skin=skin.id;else if(save.wallet>=skin.price){save.wallet-=skin.price;save.owned.push(skin.id);save.skin=skin.id;}else{systemToast(`Need ${skin.price - save.wallet} more coins`);return;}persist();render();});U.reset.addEventListener('click',()=>{if(!confirm('Reset all save data?'))return;save=defaults();persist();render();});U.play.addEventListener('click',callbacks.start);U.retry.addEventListener('click',callbacks.start);U.restart.addEventListener('click',callbacks.start);U.main.addEventListener('click',callbacks.menu);U.resultShop?.addEventListener('click',callbacks.shop);U.pause.addEventListener('click',callbacks.pause);U.resume.addEventListener('click',callbacks.resume);U.pauseMenu.addEventListener('click',callbacks.menu);U.pauseSettings.addEventListener('click',callbacks.settings);}return{U,get save(){return save;},persist,selectedSkin,audio,haptic,quip,gameplayVisible,render,openMenu,closeOverlays,bank,showGameOver,bind};}return{create,SKINS};});